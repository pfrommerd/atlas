

fn __insert(r, k, v) {
    $insert($force(r), $force(k), v)
}
fn __cons(head, tail) {
    $cons(head, tail)
}

fn __append(tup, item) {
    $append(tup, item)
}

fn __run(platform, ctx, dir, cmd, args) {
    $exec($force(platform), 
          $forceRec(ctx), 
          $force(dir),
          $force(path), 
          $force(args))
}